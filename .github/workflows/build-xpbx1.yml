name: Build xpbx1

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'hosts/nixos-arm/xpbx1/**'
      - 'overlays/**'
      - 'flake.nix'
      - 'flake.lock'

jobs:
  build:
    name: Build xpbx1
    runs-on: [self-hosted, nixos, builder]
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4

      - name: Build xpbx1
        run: |
          CACHE_URL="file:///zfs/nixcache/cache"
          echo "Building xpbx1..."
          nix build ".#nixosConfigurations.xpbx1.config.system.build.toplevel" \
            --no-link \
            --max-jobs 2 \
            --cores 4
          echo "Copying to cache..."
          nix copy --to "$CACHE_URL" ".#nixosConfigurations.xpbx1.config.system.build.toplevel"
          echo "xpbx1: BUILD SUCCESS"

      - name: Build xpbx1-minimal SD image
        run: |
          CACHE_URL="file:///zfs/nixcache/cache"
          echo "Building xpbx1-minimal SD image..."
          nix build ".#nixosConfigurations.xpbx1-minimal.config.system.build.sdImage" \
            --no-link \
            --max-jobs 2 \
            --cores 4
          echo "Copying SD image to cache..."
          nix copy --to "$CACHE_URL" ".#nixosConfigurations.xpbx1-minimal.config.system.build.sdImage"
          echo "xpbx1-minimal SD image: BUILD SUCCESS"
