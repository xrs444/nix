<!-- Autogenerated: 2026-09-01T06:55:01.976032Z -->
# users

## Files

- deploy.nix
- thomas-local.nix
- automation.nix
- default.nix
- builder.nix
- xrs444.nix
- samantha.nix
- acme.nix

## File Summaries

### deploy.nix
Dedicated deploy user for deploy-rs push-based deployments from xsvr1.

### thomas-local.nix
The orphaned unlabeled key that used to live here (matching no known private key on the fleet — same pattern as the xrs444 key replaced 2026-08-07) was retired 2026-08-25.

### automation.nix
Automation user for Windmill — SSH executor for bare-metal NixOS jobs. Imported from nix/hosts/common/default.nix so every NixOS host gets it. The corresponding SSH private key lives in flux/apps/windmill/sealedsecret-windmill-ssh-key.yaml

### default.nix
Samantha's account only exists on her own laptop, not fleet-wide like the admin accounts above.

### builder.nix
{ lib, pkgs, ... }:
{
  users.users.builder = {
    isNormalUser = true;
    description = "Remote Nix builder user";

### xrs444.nix
voxtype (homemanager/users/xrs444, xdt1-t only) types transcribed text via dotool/ydotool's uinput backend as a fallback if wtype's Wayland virtual-keyboard protocol isn't available. "uinput" only grants creating a virtual output device — unlike the "input" group, it does NOT grant reading real keyboard/mouse input, so this stays narrow even though voxtype's hotkey itself is niri-keybind-triggered, not evdev.

### samantha.nix
voxtype (homemanager/users/samantha) types transcribed text via dotool's uinput backend — GNOME's mutter doesn't support wtype's virtual-keyboard protocol, so dotool (uinput) is the path that actually runs here. "uinput" only grants creating a virtual output device — unlike the "input" group, it does NOT grant reading real keyboard/mouse input, so this stays narrow even though voxtype's hotkey itself is compositor-triggered, not evdev.

### acme.nix
Provide a default for minimalImage if not defined

## Overview

This folder contains the following files with their respective purposes summarized above.
