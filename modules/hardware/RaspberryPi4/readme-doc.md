<!-- Autogenerated: 2025-12-04T18:03:42.720291Z -->
# RaspberryPi4

## Files

- i2c.nix
- digi-amp-plus.nix
- touch-ft5406.nix
- audio.nix
- xhci.nix
- poe-hat.nix
- backlight.nix
- tv-hat.nix
- dwc2.nix
- pwm0.nix
- gpio.nix
- apply-overlays-dtmerge.nix
- bluetooth.nix
- leds.nix
- default.nix
- pkgs-overlays.nix
- tc358743.nix
- modesetting.nix
- cpu-revision.nix
- poe-plus-hat.nix

## File Summaries

### i2c.nix
{ config, lib, ... }:

### digi-amp-plus.nix
{ config, lib, ... }:

### touch-ft5406.nix
{ config, lib, ... }:

### audio.nix
{

### xhci.nix
{

### poe-hat.nix
{ config, lib, ... }:

### backlight.nix
{ config, lib, ... }:

### tv-hat.nix
{ config, lib, ... }:

### dwc2.nix
{ config, lib, ... }:

### pwm0.nix
{ config, lib, ... }:

### gpio.nix
{

### apply-overlays-dtmerge.nix
# modification of nixpkgs deviceTree.applyOverlays to resolve https://github.com/NixOS/nixpkgs/issues/125354

### bluetooth.nix
{ config, lib, ... }:

### leds.nix
{ config, lib, ... }:

### default.nix
{

### pkgs-overlays.nix
{ config, lib, ... }:

### tc358743.nix
{ config, lib, ... }:

### modesetting.nix
{ config, lib, ... }:

### cpu-revision.nix
{

### poe-plus-hat.nix
{ config, lib, ... }:

## Overview

This folder contains the following files with their respective purposes summarized above.
