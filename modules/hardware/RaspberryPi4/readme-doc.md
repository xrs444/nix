<!-- Autogenerated: 2026-09-26T06:46:37.247939Z -->
# RaspberryPi4

## Files

- touch-ft5406.nix
- cpu-revision.nix
- apply-overlays-dtmerge.nix
- bluetooth.nix
- tc358743.nix
- modesetting.nix
- digi-amp-plus.nix
- tv-hat.nix
- backlight.nix
- poe-plus-hat.nix
- default.nix
- pwm0.nix
- i2c.nix
- leds.nix
- xhci.nix
- pkgs-overlays.nix
- poe-hat.nix
- dwc2.nix
- audio.nix
- gpio.nix

## File Summaries

### touch-ft5406.nix
{ config, lib, ... }:
let
  cfg = config.hardware.raspberry-pi."4".touch-ft5406;
in
{

### cpu-revision.nix
{
  hardware.deviceTree.overlays = [
    {
      name = "rpi4-cpu-revision";
      dtsText = ''

### apply-overlays-dtmerge.nix
modification of nixpkgs deviceTree.applyOverlays to resolve https://github.com/NixOS/nixpkgs/issues/125354 derived from https://github.com/NixOS/nixpkgs/blob/916ca8f2b0c208def051f8ea9760c534a40309db/pkgs/os-specific/linux/device-tree/default.nix

### bluetooth.nix
doesn't work for the CM module, so we exclude e.g. bcm2711-rpi-cm4.dts

### tc358743.nix
address-cells = <0x01>; size-cells = <0x00>;

### modesetting.nix
fixes a crash: https://github.com/raspberrypi/linux/issues/5568 can be removed for >= nixos 23.11: https://github.com/NixOS/nixpkgs/pull/247826

### digi-amp-plus.nix
Adapted from to: https://github.com/raspberrypi/linux/blob/3ed6d34d53e94ecbebc64c8fa3d1b6d3c41db8fb/arch/arm/boot/dts/overlays/iqaudio-dacplus-overlay.dts changes: - modified top-level "compatible" field from bcm2835 to bcm2711 - s/i2s_clk_producer/i2s/ (name on bcm2711 platform)

### tv-hat.nix
https://github.com/raspberrypi/linux/blob/rpi-6.1.y/arch/arm/boot/dts/overlays/spi0-0cs-overlay.dts https://github.com/raspberrypi/linux/blob/rpi-6.1.y/arch/arm/boot/dts/overlays/rpi-tv-overlay.dts

### backlight.nix
This overlay was originally taken from: https://github.com/raspberrypi/linux/blob/rpi-6.1.y/arch/arm/boot/dts/overlays/rpi-backlight-overlay.dts The only modification made was to change the compatible field to bcm2711 this is the same as for the 5.15.y kernel

### poe-plus-hat.nix
doesn't work for the CM module, so we exclude e.g. bcm2711-rpi-cm4.dts

### default.nix
Allow building kernel

### pwm0.nix
{ config, lib, ... }:
let
  cfg = config.hardware.raspberry-pi."4".pwm0;
in
{

### i2c.nix
{ config, lib, ... }:
let
  cfg = config.hardware.raspberry-pi."4";
  optionalProperty =
    name: value: lib.optionalString (value != null) "${name} = <${builtins.toString value}>;";

### leds.nix
Adapted from: https://gist.github.com/SFrijters/206d2c09656affb04284f076c75a1969

### xhci.nix
{
  config,
  lib,
  ...
}:

### pkgs-overlays.nix
{ config, lib, ... }:
let
  cfg = config.hardware.raspberry-pi."4".apply-overlays-dtmerge;
  dt_ao_overlay = _final: prev: {
    deviceTree = prev.deviceTree // {

### poe-hat.nix
Hardware configuration for Raspberry Pi 4 POE Hat, enables device tree overlays and power management options.

### dwc2.nix
Configure for modesetting in the device tree

### audio.nix
Equivalent to dtparam=audio=on

### gpio.nix
the bit that matters to lgpio here is "${pkgs.coreutils}/bin/chgrp gpio /dev/%k && chmod 660 /dev/%k" see https://github.com/NixOS/nixpkgs/pull/352308

## Overview

This folder contains the following files with their respective purposes summarized above.
