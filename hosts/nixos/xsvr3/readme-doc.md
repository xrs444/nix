<!-- Autogenerated: 2026-09-26T06:46:37.280934Z -->
# xsvr3

## Files

- minimal.nix
- disks.nix
- network.nix
- desktop.nix
- default.nix
- serial.nix
- vms.nix

## File Summaries

### minimal.nix
Minimal installer configuration for xsvr3; deploy with deploy-rs after boot.

### disks.nix
{
  disko.devices = {
    disk = {
      main = {
        device = "/dev/disk/by-id/nvme-CT500P3PSSD8_241748806E5C";

### network.nix
Termix (k8s pod on 172.20.3.0/24) was tripping fail2ban's maxretry=3 sshd jail while the routing issue to the main IP was being worked out.

### desktop.nix
Use XFCE instead of GNOME/LXQt to avoid heavy build dependencies

### default.nix
NixOS host configuration for xsvr3, imports hardware, boot, desktop, and VM modules.

### serial.nix
Define your USB device mappings here

### vms.nix
virtio disks are PCI devices -- a 'drive'-type address (meant for IDE/SATA controller addressing) is invalid on them and libvirt rejects the whole XML. Let libvirt auto-assign a PCI slot instead.

## Overview

This folder contains the following files with their respective purposes summarized above.
