<!-- Autogenerated: 2026-09-26T06:46:37.281494Z -->
# xsvr2

## Files

- offsite-backup.nix
- minimal.nix
- disks.nix
- network.nix
- default.nix
- replication.nix
- vms.nix

## File Summaries

### offsite-backup.nix
Offsite backup for xsvr2 — restic via SFTP to Synology  Backs up critical datasets received from xsvr1 replication. Rate limited to 10 Mbps (1.25 MiB/s) to leave headroom on 30 Mbps WAN. Initial seed (~300 GB) will take ~67 days at full rate — restic is resumable.  Prerequisites (one-time manual steps): 1. Create sops secret file: sops nix/secrets/restic-offsite.yaml Add keys: password (restic repo password), ssh_key (ed25519 private key) 2. On Synology: create user 'restic', enable SSH, add public key 3. Update synologyHost below to the actual Synology FQDN/IP 4. Initialize repo on first run: restic -r sftp:restic@<synology-host>:/volume1/restic-xsvr2 init

### minimal.nix
Minimal installer configuration for xsvr2; deploy with deploy-rs after boot.

### disks.nix
{
  disko.devices = {
    disk = {
      one = {
        type = "disk";

### network.nix
Termix (k8s pod on 172.20.3.0/24) was tripping fail2ban's maxretry=3 sshd jail while the routing issue to the main IP was being worked out.

### default.nix
NixOS host configuration for xsvr2, imports hardware, boot, VM, and disk modules.

### replication.nix
ZFS Replication Configuration for xsvr2 (Target Host) Receives replicated datasets from xsvr1

### vms.nix
virtio disks are PCI devices -- a 'drive'-type address (meant for IDE/SATA controller addressing) is invalid on them and libvirt rejects the whole XML. Let libvirt auto-assign a PCI slot instead.

## Overview

This folder contains the following files with their respective purposes summarized above.
