<!-- Autogenerated: 2026-08-14T00:00:00.000000Z -->
# xsvr4

## Files

- minimal.nix
- vms.nix
- desktop.nix
- disks.nix
- default.nix
- network.nix

## File Summaries

### minimal.nix
{ pkgs, inputs, ... }:

### vms.nix
{ pkgs, lib, ... }:

### desktop.nix
{ config, pkgs, ... }:

### disks.nix
{ lib, ... }:

### default.nix
{

### network.nix
{ pkgs, lib, ... }:

## Overview

This folder contains the following files with their respective purposes summarized above.
Templated from xsvr3 (same spec: 5 vcpu / 24 GiB Talos VM on bridge22). No serial.nix — xsvr4
has no USB-serial console adapters mapped yet; add one if/when the same cabling is replicated.
