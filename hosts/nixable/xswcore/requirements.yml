---
# Ansible collections required for xswcore (Brocade ICX-7250) management
# Install with: just install-collections
collections:
  - name: ansible.netcommon
    version: ">=5.0.0"
  - name: community.network
    version: ">=5.1.0"
